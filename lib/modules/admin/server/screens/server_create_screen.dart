import 'package:flutter/material.dart';

class ServerCreateScreen extends StatelessWidget {
  const ServerCreateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Create Server'),
      ),
    );
  }
}
